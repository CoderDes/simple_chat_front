import React from "react";

import Message from "./Message";

const MessageList = props => (
  <ul>
    {/* props.messages.map(message => <li><Message author, text /></li>) */}
  </ul>
);

export default MessageList;
